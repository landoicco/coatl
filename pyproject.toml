[project]
name = "PySnake"
version = "0.1.0"
description = "A classic snake like clone made with Python and Pygame"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "pygame>=2.6.1",
]

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]
